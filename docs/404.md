---
title: 404 Not Found
---

<script>
  (function() {
    // Repo subpath used on GitHub Pages
    const repoBase = '/welding-techniques-by-Cloude-AI';
    const path = window.location.pathname || '/';
    // Match common language root paths like /en/, /zh/, /ar/ or /ru/ and preserve remainder
    const m = path.match(/^\/(en|zh|ar|ru)(\/.*|$)/i);
    if (m) {
      const lang = m[1].toLowerCase();
      const tail = m[2] || '/';
      // Normalize double slashes
      const target = `${repoBase}/${lang}${tail}`.replace(/([^:])(\/){2,}/g, '$1/$2');
      window.location.replace(target);
      return;
    }
    // If user visits domain root (like https://username.github.io/), redirect to the project base
    if (path === '/' || path === '') {
      window.location.replace(repoBase + '/');
      return;
    }
  })();
</script>

<noscript>
  <meta http-equiv="refresh" content="0;url=https://nickscherbakov.github.io/welding-techniques-by-Cloude-AI/en/">
</noscript>

# 404 — File not found

It looks like the page you're trying to reach isn't available on this site.

If you were trying to view the English documentation, you can follow this link instead:

- [English documentation](https://nickscherbakov.github.io/welding-techniques-by-Cloude-AI/en/)

Or go to the project root:

- [Project home](https://nickscherbakov.github.io/welding-techniques-by-Cloude-AI/)
